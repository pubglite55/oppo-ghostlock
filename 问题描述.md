# GhostLock OPPO Find N2 — 问题描述

## 项目背景

GhostLock (CVE-2026-43499) 是一个影响 Linux 2.6.39 到 7.1 的内核栈 UAF 漏洞，通过 `FUTEX_CMP_REQUEUE_PI` 竞争条件触发。本项目基于 NebuSec CyberMeowfia 开源研究项目，针对 OPPO Find N2 (SM8475/CPH2413, kernel 5.10.236) 进行适配。

## 当前状态

### 已完成

| 阶段 | 状态 | 说明 |
|------|------|------|
| Firefox CVE-2026-10702 | ✅ | SpiderMonkey type confusion → fake TypedArray → AAW |
| KASLR bypass | ✅ | pselect side-channel 泄漏 nfulnl_logger |
| GhostLock FUTEX PI 触发 | ✅ | FUTEX_CMP_REQUEUE_PI ret=1 |
| KernelSnitch mm_struct 泄漏 | ✅ | pile-up ✅, hashsize ✅, bruteforce ✅ |
| sk_buff reclaim | ✅ | 4/4 send 成功 |
| PR #11 merged | ✅ | boot_id 数据偏移修正 |
| PR #12 merged | ✅ | P0_PAGE_OFFSET 和 P0_KERNEL_PHYS_LOAD 修正 |
| PR #13 merged | ✅ | bypass slide, direct-map kernel base |

### 阻塞

| 阶段 | 状态 | 说明 |
|------|------|------|
| slide pselect (栈覆盖) | ❌ **不可行** | waiter 在 fd_set 数据下方 120 字节，fd_set bitmaps 无法到达 waiter 位置 |
| configfs R/W | ❌ **死路** | ashmem strcpy 行为 |
| pipe physrw | ⏳ | 依赖栈覆盖修复 |
| root (cred + SELinux) | ⏳ | 依赖 pipe physrw |

## 核心问题

### slide pselect 根因分析

**问题**: waiter 偏移错位 120 字节

**精确偏移计算 (IDA 验证)**:
```
pselect 帧链: pselect6(0xA0) + core_sys_select(0x1C0) + do_select(0x3C0) = 0x620
core_sys_select SP = stack_top - 0x260
fd_set 数据 (v35) = SP + 0x50 = stack_top - 0x210
waiter 结构 = stack_top - 0x288
偏移差 = 0x288 - 0x210 = 0x78 (120 bytes)
```

**根因**: waiter 在 fd_set 数据**下方** 120 字节。fd_set bitmaps 从 stack_top - 0x210 开始向**上**增长，无法覆盖到 stack_top - 0x288 的 waiter 位置。

**`FRONTEND_STACK_ALLOC=256`** 确认阈值为 42.67 bytes，NFDS=320 是栈路径最大值。

### NFDS 扫描结果

| NFDS | v17 | 路径 | 结果 |
|------|-----|------|------|
| 320 | 40 | 栈缓冲区 | crash — waiter 在 fd_set 下方 120B |
| 321 | 48 | kvmalloc | crash — fd_set 不在栈上 |
| 344 | 48 | kvmalloc | crash — fd_set 不在栈上 |
| 640 | 80 | kvmalloc | crash — fd_set 不在栈上 |

**结论**: 没有 NFDS 值能让 fd_set 覆盖 waiter 位置。需要完全重新设计 slide 机制。

## 下一步方向

1. **sendmsg** — `__sys_sendmsg` 帧大小 0x314，复制 msghdr+cmsg 从用户到内核栈。需要 IDA 分析栈布局。
2. **其他 syscall** — 寻找能将用户可控数据放置到 waiter 位置的 syscall
3. **重新设计 GhostLock trigger** — 不依赖 pselect 栈覆盖

## 参考资料

- NebuSec writeup: https://nebusec.ai/research/ionstack-part-2/
- K80U PR #22: https://github.com/NebuSec/CyberMeowfia/pull/22
- Dere3046 专家建议: "c ashmem更简单 最终应该只需要适配部分就可以复现"
